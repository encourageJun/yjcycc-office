<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.yjcycc.office.mapper.FileInfoMapper">
	
	<!-- table name -->
	<sql id="tableName">
		yjcycc_file_info
	</sql>
	
	<!-- select field -->
	<sql id="selectField">
		id,
		file_name AS fileName,
		file_type AS fileType,
		remark,
		file_owner AS fileOwner,
		create_date AS createDate,
		parent_id AS parentId,
		file_path AS filePath,
		if_delete AS ifDelete
	</sql>
	
	<!-- add field -->
	<sql id="addField">
		id,
		file_name,
		file_type,
		remark,
		file_owner,
		create_date,
		parent_id,
		file_path,
		if_delete
	</sql>
	
	<!-- field values -->
	<sql id="fieldValue">
		#{id},
		#{fileName},
		#{fileType},
		#{remark},
		#{fileOwner},
		#{createDate},
		#{parentId},
		#{filePath},
		#{ifDelete}
	</sql>
	
	<!-- getByEntity -->
	<select id="get" resultType="fileInfo" parameterType="fileInfo">
		SELECT 
		<include refid="selectField"></include>
		FROM 
		<include refid="tableName"></include>
		WHERE 1 = 1
		<if test="id != null and id != ''">
			AND id = #{id}
		</if>
		<if test="fileName != null and fileName != ''">
			AND file_name = #{fileName}
		</if>
		limit 1
	</select>
	
	<!-- getByMap -->
	<select id="getByMap" resultType="fileInfo" parameterType="map">
		SELECT 
		<include refid="selectField"></include>
		FROM 
		<include refid="tableName"></include>
		WHERE 1 = 1
		<if test="id != null and id != ''">
			AND id = #{id}
		</if>
		<if test="fileName != null and fileName != ''">
			AND file_name = #{fileName}
		</if>
		limit 1
	</select>
	
	<!-- 分页, 使用了pagehelper插件 -->
	<select id="findPager" resultType="fileInfo" parameterType="fileInfo">
		SELECT 
		<include refid="selectField"></include>
		FROM 
		<include refid="tableName"></include>
        WHERE 1 = 1
        	<!-- AND id = #{id} -->
        <if test="fileType != null">
        	AND file_type = #{fileType}
        </if>
        <if test="parentId != null">
        	AND parent_id = #{parentId}
        </if>
        
        ORDER BY create_date desc
	</select>
	
	<!-- 分页, 使用了pagehelper插件 -->
	<select id="findPagerByMap" resultType="fileInfo" parameterType="map">
		SELECT 
		<include refid="selectField"></include>
		FROM 
		<include refid="tableName"></include>
        WHERE 1 = 1
        	<!-- AND id = #{id} -->
        <if test="fileType != null">
        	AND file_type = #{fileType}
        </if>
        <if test="parentId != null">
        	AND parent_id = #{parentId}
        </if>
        ORDER BY create_date desc
	</select>
	
	<!-- insert -->
	<insert id="insert" parameterType="fileInfo">
		INSERT INTO 
		<include refid="tableName"></include>
		(
		<include refid="addField"></include>
		) VALUES (
		<include refid="fieldValue"></include>
		)
	</insert>
	
	<!-- updateByEntity -->
	<update id="update" parameterType="fileInfo">
		UPDATE <include refid="tableName"></include>
		SET
			file_name = #{fileName}
		WHERE
			id = #{id}
	</update>
	
	<!-- updateByMap -->
	<update id="updateByMap" parameterType="map">
		UPDATE <include refid="tableName"></include>
		SET
			file_name = #{fileName}
		WHERE
			id = #{id}
	</update>
	
	<!-- delete -->
	<delete id="delete" parameterType="fileInfo">
		DELETE FROM <include refid="tableName"></include>
		WHERE
			id = #{id}
	</delete>
	
	<!-- batch delete -->
	<delete id="batchDelete" parameterType="String">
		DELETE FROM <include refid="tableName"></include>
		WHERE
			id in (${ids})
	</delete>
	
</mapper>